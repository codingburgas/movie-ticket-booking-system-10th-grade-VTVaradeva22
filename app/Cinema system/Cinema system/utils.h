#pragma once

#include <iostream>
#include <string>
#include <iomanip>

using namespace std;

// Prints the text centered in the console window
void centerText(const string& text);

// Prints empty lines
void printEndl(int n);

// Prints a string multiple times
void printStrRepeat(const string& str, int count);

// Clears the console screen
void clearScreen();